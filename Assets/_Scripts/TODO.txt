TODO:
Reset should stay within the current scene, not reload it.

TODO:
TrackStats() needs to keep track of frames inAir, onBack, Boosting, AtMax
after each interactable and keep an average of these stats for each interactable and level

TODO:
Create moon light and write script to rotate sun and moon

FIXME:
While player is in the air or touching an interactable, the points they get are locally stored,
when the player leaves object or touches ground, the points they racked up are then given to the
lastObjectTouched.